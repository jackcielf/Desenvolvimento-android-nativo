package com.devjakkifx.projeto1

class Mensagem {
    fun enviarNovaMensagem() {
       // val usuario = Usuario();
       // usuario.autenticar();
        Usuario().autenticar(); // Outra forma de fazer a mesma coisa acima
    }
}